name: Nix CI

on:
  push:
  pull_request:

jobs:
  build-and-test:
    strategy:
      # Run tests on all OS's and HHVM versions, even if one fails
      fail-fast: false
      matrix:
        os:
        - ubuntu-latest
        - macos-latest
    runs-on: ${{matrix.os}}
    steps:
    - uses: actions/checkout@v2.4.0
    - uses: cachix/install-nix-action@v15
      with:
        extra_nix_config: |
          access-tokens = github.com=${{ secrets.GITHUB_TOKEN }}
          experimental-features = nix-command flakes
    - run: nix profile install --print-build-logs --no-sandbox '.?submodules=1&shallow=1'
    - run: nix bundle --bundler github:NixOS/bundlers#toDEB '.?submodule=1&shallow=1'
    - run: dpkg -L */*.deb
    - run: nix flake check --print-build-logs '.?submodules=1&shallow=1'
    - run: hhvm --version
